import 'package:flutter/material.dart';

class TranslateMain extends StatelessWidget {
  const TranslateMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
